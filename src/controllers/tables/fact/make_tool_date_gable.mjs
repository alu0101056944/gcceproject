


// should be relative, run it once a day and get the version info
// interest_level (google trends), pass the version through a function
// to decide the change type.

export default async function makeToolDateTable(toolTable, dateTable) {

  

}

(async () => {
  await makeToolDateTable();
})();
